# Check if the pypdf module is installed

try:
    import pypdf
    print("pypdf installed")
except ImportError:
    print("pypdf not installed")
    print("Please look through README.md for installation instructions.")

# Merge PDFs using pypdf 6.x
from pypdf import PdfWriter
import os

# List your PDF files here
files = [
    r"./pdfs/file1.pdf",
    r"./pdfs/file2.pdf"
]

# Check existence
for f in files:
    if not os.path.exists(f):
        raise FileNotFoundError(f" File not found: {f}")
    else:
        print(f"Found: {f}")

# Create output folder
os.makedirs("./output", exist_ok=True)

# Merge
writer = PdfWriter()

for pdf in files:
    writer.append(pdf)

output_path = "./output/merged.pdf"
writer.write(output_path)

print("🎉 Merge complete! Output:", output_path)
